# Upload To GitHub

Your repository is:

```text
https://github.com/abedking361-lgtm/Egydead
```

GitHub rejected the automatic commit with `403 Resource not accessible by integration`, so upload these files manually:

1. Open the repository.
2. Press **Add file**.
3. Choose **Upload files**.
4. Upload everything inside this folder.
5. Commit to `main`.

After that, use the module inside an Aniyomi extensions build repo.
